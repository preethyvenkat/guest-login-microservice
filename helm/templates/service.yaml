apiVersion: v1
kind: Service
metadata:
  name: guest-login-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: guest-login-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
